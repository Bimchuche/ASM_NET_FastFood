@{
    ViewData["Title"] = "Đặt lại mật khẩu";
    Layout = null;
    var email = ViewBag.Email ?? "";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Đặt lại mật khẩu - FastFood</title>
    <link href="~/client/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/client/css/auth.css" rel="stylesheet" />
</head>
<body class="auth-page">
    <!-- Floating Icons -->
    <div class="floating-icon">🍔</div>
    <div class="floating-icon">🍟</div>
    <div class="floating-icon">🍕</div>
    <div class="floating-icon">🌭</div>
    <div class="floating-icon">🍗</div>
    
    <!-- Back to Login -->
    <a href="/Account/Login" class="back-home">
        ← Quay lại đăng nhập
    </a>
    
    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-header">
                <div class="auth-logo">🔑</div>
                <h1 class="auth-title">Đặt lại mật khẩu</h1>
                <p class="auth-subtitle">Nhập mã 6 số đã gửi đến email của bạn</p>
            </div>
            
            @if (ViewBag.Error != null)
            {
                <div class="auth-alert danger">
                    ⚠️ @ViewBag.Error
                </div>
            }
            
            <form method="post" action="/Account/ResetPassword" class="auth-form">
                <div class="form-floating">
                    <input type="email" class="form-control" id="email" name="email" 
                           value="@email" placeholder="Email" required />
                    <label for="email">📧 Email</label>
                </div>
                
                <p class="code-label">Mã xác nhận 6 số</p>
                <div class="form-floating" style="margin-bottom: 20px;">
                    <input type="text" class="form-control code-input" id="code" name="code" 
                           maxlength="6" pattern="\d{6}" placeholder="000000" required 
                           oninput="this.value = this.value.replace(/[^0-9]/g, '')" />
                    <label for="code" style="display: none;">Mã xác nhận</label>
                </div>
                
                <div class="form-floating">
                    <input type="password" class="form-control" id="newPassword" name="newPassword" 
                           placeholder="Mật khẩu mới" required minlength="6" />
                    <label for="newPassword">🔒 Mật khẩu mới</label>
                </div>
                
                <button type="submit" class="btn-auth">
                    ✅ Đặt lại mật khẩu
                </button>
            </form>
            
            <div class="auth-divider">
                <span>chưa nhận được mã?</span>
            </div>
            
            <p class="auth-link">
                <a href="/Account/ForgotPassword">Gửi lại mã</a>
            </p>
        </div>
    </div>
</body>
</html>
