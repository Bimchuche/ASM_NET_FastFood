<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Shipper FastFood</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/shipper/css/shipper.css" rel="stylesheet" />
    
    <!-- GLOBAL FONT: Times New Roman Bold -->
    <link href="~/css/global-font.css" rel="stylesheet" />
    <!-- Page Loader -->
    <link href="~/client/css/page-loader.css" rel="stylesheet" />
</head>

<body class="shipper-body">
    <!-- LOADING OVERLAY -->
    <div id="page-loader" class="page-loader">
        <div class="loader-float-item">🚚</div>
        <div class="loader-float-item">📦</div>
        <div class="loader-float-item">📍</div>
        <div class="loader-float-item">🏁</div>
        <div class="loader-float-item">✅</div>
        <div class="loader-float-item">📱</div>
        
        <div class="loader-content">
            <div class="loader-main">
                <div class="loader-ring"></div>
                <div class="loader-ring"></div>
                <div class="loader-ring"></div>
                <div class="loader-icon">🚚</div>
            </div>
            <div class="loader-brand">SHIPPER</div>
            <div class="loader-text">Đang tải</div>
            <div class="loader-progress">
                <div class="loader-progress-bar"></div>
            </div>
        </div>
    </div>

    @{
        var currentController = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
        var currentAction = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
    }

    <!-- NAVBAR -->
    <nav class="shipper-navbar">
        <a href="/" class="shipper-brand">
            <span class="icon">🚴</span>
            <span>FastFood Shipper</span>
        </a>
        
        <div class="shipper-nav-links">
            <a href="/Shipper/Dashboard" class="shipper-nav-btn light @(currentController == "Dashboard" ? "active" : "")">
                📋 Đơn chờ giao
            </a>
            <a href="/Shipper/Orders/MyOrders" class="shipper-nav-btn warning @(currentAction == "MyOrders" ? "active" : "")">
                📦 Đơn của tôi
            </a>
            <a href="/Shipper/Orders/History" class="shipper-nav-btn @(currentAction == "History" ? "active" : "")" style="background: #e0f2fe; color: #0369a1;">
                📊 Lịch sử
            </a>
            <form method="post" action="/Account/Logout" style="display: inline;">
                <button type="submit" class="shipper-nav-btn danger">
                    🚪 Đăng xuất
                </button>
            </form>
        </div>
    </nav>

    <!-- CONTENT -->
    <div class="shipper-content">
        @RenderBody()
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/client/js/popup.js"></script>
    <!-- Page Loader -->
    <script src="~/client/js/page-loader.js"></script>
    @RenderSection("Scripts", required: false)

</body>

</html>