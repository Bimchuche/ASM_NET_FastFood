@model List<ASM1_NET.Models.Food>
@{
    ViewData["Title"] = "Món ăn đã xóa";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="trash-list-page">
    <div class="page-header d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2>🍔 Món ăn đã xóa</h2>
            <p class="text-muted">@Model.Count items trong thùng rác</p>
        </div>
        <a asp-action="Index" class="btn btn-outline-secondary">← Quay lại</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">✅ @TempData["Success"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">⚠️ @TempData["Error"]</div>
    }

    @if (Model.Any())
    {
        <!-- Hidden forms for bulk actions -->
        <form id="bulkRestoreForm" asp-action="BulkRestoreFoods" method="post" style="display:none;">
            @Html.AntiForgeryToken()
            <div id="restoreIds"></div>
        </form>
        <form id="bulkDeleteForm" asp-action="BulkDeleteFoods" method="post" style="display:none;">
            @Html.AntiForgeryToken()
            <div id="deleteIds"></div>
        </form>

        <div class="bulk-actions mb-3">
            <button type="button" class="btn btn-success" onclick="submitBulk('restore')">
                ♻️ Khôi phục đã chọn
            </button>
            <button type="button" class="btn btn-danger" onclick="submitBulk('delete')">
                🗑️ Xóa vĩnh viễn đã chọn
            </button>
            <span class="ms-3 text-muted">Đã chọn: <strong id="selectedCount">0</strong></span>
        </div>

        <div class="table-card">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th style="width: 40px;"><input type="checkbox" id="selectAll" onclick="toggleAll(this)"></th>
                        <th>Ảnh</th>
                        <th>Tên món</th>
                        <th>Danh mục</th>
                        <th>Giá</th>
                        <th>Ngày xóa</th>
                        <th style="width: 200px;">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var food in Model)
                    {
                        <tr>
                            <td><input type="checkbox" class="item-checkbox" data-id="@food.Id" onclick="updateCount()"></td>
                            <td><img src="@food.ImageUrl" style="width:50px;height:50px;object-fit:cover;border-radius:8px;"></td>
                            <td><strong>@food.Name</strong></td>
                            <td>@food.Category?.Name</td>
                            <td>@food.Price.ToString("N0")₫</td>
                            <td>@food.DeletedAt?.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>
                                <form asp-action="RestoreFood" asp-route-id="@food.Id" method="post" style="display:inline;">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-success btn-sm">♻️</button>
                                </form>
                                <form asp-action="PermanentDeleteFood" asp-route-id="@food.Id" method="post" style="display:inline;"
                                      onsubmit="return confirm('Xóa vĩnh viễn món này?')">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-danger btn-sm">🗑️</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state"><p>Không có món ăn nào trong thùng rác</p></div>
    }
</div>

<link rel="stylesheet" href="~/admin/css/trash.css" />
<script src="~/admin/js/admin-trash.js"></script>
<script>
function submitBulk(action) {
    window.submitBulk(action, 'món ăn');
}
</script>
