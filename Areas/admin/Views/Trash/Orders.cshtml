@model List<ASM1_NET.Models.Order>
@{
    ViewData["Title"] = "Đơn hàng đã xóa";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="trash-list-page">
    <div class="page-header d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2>📦 Đơn hàng đã xóa</h2>
            <p class="text-muted">@Model.Count items trong thùng rác</p>
        </div>
        <a asp-action="Index" class="btn btn-outline-secondary">← Quay lại</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">✅ @TempData["Success"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">⚠️ @TempData["Error"]</div>
    }

    @if (Model.Any())
    {
        <form id="bulkRestoreForm" asp-action="BulkRestoreOrders" method="post" style="display:none;">
            @Html.AntiForgeryToken()
            <div id="restoreIds"></div>
        </form>
        <form id="bulkDeleteForm" asp-action="BulkDeleteOrders" method="post" style="display:none;">
            @Html.AntiForgeryToken()
            <div id="deleteIds"></div>
        </form>

        <div class="bulk-actions mb-3">
            <button type="button" class="btn btn-success" onclick="submitBulk('restore')">♻️ Khôi phục đã chọn</button>
            <button type="button" class="btn btn-danger" onclick="submitBulk('delete')">🗑️ Xóa vĩnh viễn đã chọn</button>
            <span class="ms-3 text-muted">Đã chọn: <strong id="selectedCount">0</strong></span>
        </div>

        <div class="table-card">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th style="width: 40px;"><input type="checkbox" id="selectAll" onclick="toggleAll(this)"></th>
                        <th>Mã đơn</th>
                        <th>Khách hàng</th>
                        <th>Tổng tiền</th>
                        <th>Trạng thái</th>
                        <th>Ngày xóa</th>
                        <th style="width: 200px;">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model)
                    {
                        <tr>
                            <td><input type="checkbox" class="item-checkbox" data-id="@order.Id" onclick="updateCount()"></td>
                            <td><strong>@order.OrderCode</strong></td>
                            <td>@order.Customer?.FullName</td>
                            <td>@order.TotalAmount.ToString("N0")₫</td>
                            <td><span class="badge bg-secondary">@order.Status</span></td>
                            <td>@order.DeletedAt?.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>
                                <form asp-action="RestoreOrder" asp-route-id="@order.Id" method="post" style="display:inline;">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-success btn-sm">♻️</button>
                                </form>
                                <form asp-action="PermanentDeleteOrder" asp-route-id="@order.Id" method="post" style="display:inline;"
                                      onsubmit="return confirm('Xóa vĩnh viễn đơn hàng này?')">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-danger btn-sm">🗑️</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state"><p>Không có đơn hàng nào trong thùng rác</p></div>
    }
</div>

<link rel="stylesheet" href="~/admin/css/trash.css" />
<script src="~/admin/js/admin-trash.js"></script>
<script>
function submitBulk(action) {
    window.submitBulk(action, 'đơn hàng');
}
</script>
