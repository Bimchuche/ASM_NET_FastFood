@model IEnumerable<ASM1_NET.Models.Combo>

@{
    ViewData["Title"] = "Quản lý Combo";
}

<!-- ALERTS -->
@if (TempData["Success"] != null)
{
    <div class="admin-alert success">✅ @TempData["Success"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="admin-alert danger">⚠️ @TempData["Error"]</div>
}

<!-- Hidden form for bulk delete -->
<form id="bulkDeleteForm" asp-action="BulkDelete" method="post" style="display:none;">
    @Html.AntiForgeryToken()
    <div id="deleteIds"></div>
</form>

<!-- HEADER -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="mb-1">🎁 Quản lý Combo</h2>
        <p class="text-muted mb-0">Tổng: <strong>@Model.Count()</strong> combo</p>
    </div>
    <div class="d-flex gap-2">
        <button type="button" class="btn-admin danger" onclick="submitBulkDelete()">
            🗑️ Xóa đã chọn (<span id="selectedCount">0</span>)
        </button>
        <a asp-action="Create" class="btn-admin primary">
            <span>➕</span> Thêm combo mới
        </a>
    </div>
</div>

<!-- TABLE CARD -->
<div class="admin-table-card">
    <div class="table-header">
        <h3>📋 Danh sách combo</h3>
        <div class="table-search">
            <input type="text" id="searchCombo" placeholder="Tìm kiếm..." onkeyup="filterTable()" />
        </div>
    </div>
    
    <table class="admin-table" id="comboTable">
        <thead>
            <tr>
                <th style="width: 40px;"><input type="checkbox" id="selectAll" onclick="toggleAll(this)"></th>
                <th style="width: 80px;">Ảnh</th>
                <th>Tên combo</th>
                <th>Mô tả</th>
                <th>Giá</th>
                <th>Trạng thái</th>
                <th style="width: 150px;">Hành động</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var combo in Model)
            {
                <tr>
                    <td><input type="checkbox" class="item-checkbox" data-id="@combo.Id" onclick="updateCount()"></td>
                    <td>
                        @if (!string.IsNullOrEmpty(combo.ImageUrl))
                        {
                            <img src="@combo.ImageUrl" alt="@combo.Name" />
                        }
                        else
                        {
                            <div class="stat-icon warning" style="width: 50px; height: 50px; font-size: 1.2rem;">🎁</div>
                        }
                    </td>
                    <td>
                        <strong>@combo.Name</strong>
                    </td>
                    <td>
                        <small class="text-muted">@(combo.Description?.Length > 50 ? combo.Description.Substring(0, 50) + "..." : combo.Description ?? "Chưa có mô tả")</small>
                    </td>
                    <td>
                        <strong class="text-success">@combo.Price.ToString("N0") ₫</strong>
                    </td>
                    <td>
                        @if (combo.IsActive)
                        {
                            <span class="badge-admin success">✓ Đang bán</span>
                        }
                        else
                        {
                            <span class="badge-admin danger">✕ Ngừng bán</span>
                        }
                    </td>
                    <td>
                        <div class="action-btns">
                            <a asp-action="Edit" asp-route-id="@combo.Id" class="action-btn edit" title="Sửa">
                                ✏️
                            </a>
                            <a asp-action="Delete" asp-route-id="@combo.Id" class="action-btn delete" title="Xóa"
                               onclick="return confirm('Bạn có chắc muốn xóa combo này?')">
                                🗑️
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
<script src="~/admin/js/admin-table.js"></script>
<script>
function filterTable() { 
    window.filterTable('searchCombo', 'comboTable'); 
}
function submitBulkDelete() {
    window.submitBulkDelete('combo');
}
</script>
}