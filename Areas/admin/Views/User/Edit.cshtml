@model ASM1_NET.Models.User
@{
    ViewData["Title"] = "Sửa người dùng";
}

<!-- Page Header -->
<div class="admin-page-header">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
        <div>
            <h2 class="admin-page-title">✏️ Sửa người dùng</h2>
            <p class="admin-page-subtitle">Chỉnh sửa thông tin người dùng #@Model.Id</p>
        </div>
        <a asp-action="Index" class="btn btn-admin btn-admin-back">
            ⬅️ Quay lại
        </a>
    </div>
</div>

<!-- Form Card -->
<div class="admin-form-card">
    <div class="form-card-header">
        <h4>📝 Thông tin người dùng</h4>
    </div>
    <div class="form-card-body">
        <form method="post" asp-action="Edit">
            <input type="hidden" asp-for="Id" />
            
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">👤</span> Họ tên <span class="required">*</span>
                        </label>
                        <input asp-for="FullName" class="form-control" placeholder="Nhập họ tên..." />
                        <span asp-validation-for="FullName" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">📧</span> Email <span class="required">*</span>
                        </label>
                        <input asp-for="Email" class="form-control" placeholder="Nhập email..." />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                </div>
            </div>
            
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">🔐</span> Mật khẩu (để trống nếu không đổi)
                        </label>
                        <input asp-for="Password" type="password" class="form-control" placeholder="Nhập mật khẩu mới..." />
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">📞</span> Số điện thoại
                        </label>
                        <input asp-for="Phone" class="form-control" placeholder="Nhập SĐT..." />
                        <span asp-validation-for="Phone" class="text-danger"></span>
                    </div>
                </div>
            </div>
            
            <div class="form-group">
                <label class="form-label">
                    <span class="label-icon">📍</span> Địa chỉ
                </label>
                <input asp-for="Address" class="form-control" placeholder="Nhập địa chỉ..." />
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>
            
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">🎭</span> Vai trò
                        </label>
                        <select asp-for="Role" class="form-select">
                            <option value="Customer">🛒 Khách hàng</option>
                            <option value="Shipper">🚚 Shipper</option>
                            <option value="Admin">👑 Admin</option>
                        </select>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">⚡</span> Trạng thái
                        </label>
                        <div class="form-check-wrapper mt-2">
                            <label class="form-check-label">
                                <input asp-for="IsActive" class="form-check-input" />
                                <span>Tài khoản đang hoạt động</span>
                            </label>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn btn-admin btn-admin-save">
                    💾 Cập nhật
                </button>
                <a asp-action="Index" class="btn btn-admin btn-admin-cancel">
                    ❌ Hủy bỏ
                </a>
            </div>
        </form>
    </div>
</div>