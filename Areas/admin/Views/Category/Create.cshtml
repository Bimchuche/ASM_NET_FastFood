@model ASM1_NET.Models.Category
@{
    ViewData["Title"] = "Thêm danh mục";
}

<!-- Page Header -->
<div class="admin-page-header">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
        <div>
            <h2 class="admin-page-title">➕ Thêm danh mục mới</h2>
            <p class="admin-page-subtitle">Tạo danh mục sản phẩm mới</p>
        </div>
        <a asp-action="Index" class="btn btn-admin btn-admin-back">
            ⬅️ Quay lại
        </a>
    </div>
</div>

<!-- Form Card -->
<div class="admin-form-card">
    <div class="form-card-header">
        <h4>📝 Thông tin danh mục</h4>
    </div>
    <div class="form-card-body">
        <form method="post" asp-action="Create">
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">📁</span> Tên danh mục <span class="required">*</span>
                        </label>
                        <input asp-for="Name" class="form-control" placeholder="Nhập tên danh mục..." />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label class="form-label">
                            <span class="label-icon">⚡</span> Trạng thái
                        </label>
                        <select asp-for="IsActive" class="form-select">
                            <option value="true">✅ Hiển thị</option>
                            <option value="false">🚫 Ẩn</option>
                        </select>
                    </div>
                </div>
            </div>
            
            <div class="form-group">
                <label class="form-label">
                    <span class="label-icon">📝</span> Mô tả
                </label>
                <textarea asp-for="Description" class="form-control" rows="4" placeholder="Nhập mô tả danh mục..."></textarea>
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn btn-admin btn-admin-save">
                    💾 Lưu danh mục
                </button>
                <a asp-action="Index" class="btn btn-admin btn-admin-cancel">
                    ❌ Hủy bỏ
                </a>
            </div>
        </form>
    </div>
</div>